mutation UpdateContest($contestId: ID!, $input: ContestInput!) {
  updateContest(id: $contestId, input: $input) {
    id
    contest_name
    contest_no
    total_price
    total_amount
    type
    status
  }
}
